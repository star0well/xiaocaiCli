import { reactive, ref } from "vue";

export const contentTableConfig = (width) =>
  reactive({
    propList: [
      { prop: "field_name", label: "分类名称" },
      {
        label: "操作",
        prop: "pid",
        width,
        slotName: "operation",
      },
    ],
    showIndexColumn: false,
    showSelectColumn: true,
    childrenProps: {},
  });
export const searchList = [
  {
    type: "input",
    placeholder: "请输入分类名称",
    field: "field_name",
    label: "分类名称:",
  },
];

export const current = ref(0);

export const modalList = () =>
  reactive([
    {
      type: "input",
      label: "分类名称",
      field: "field_name",
      requireRules: true, //是否使用rules
      placeholder: "请输入分类名称",
      rules: [{ required: true, message: "请输入分类名称", trigger: "blur" }],
      span: 24,
      disabled: current.value == 2,
    },
  ]);
